<% if fclass.class_img.file.nil? %>
  <%= link_to deck_path("#{fclass.title.downcase.gsub(' ', '-')}-#{current_user.id}"),
    class: "relative cursor-pointer text-3xl #{active_fclass?(fclass.title) ? 'text-emerald-500 hover:text-emerald-600' : 'text-slate-400 hover:text-slate-500'} flex items-center justify-center gap-2 w-3/4 my-1" do %>
    <i class="fa-solid fa-clone"></i>
    <span class="hidden text-xl ms-auto w-3/4 font-semibold" data-sidebar-target="fclass"><%= fclass.title %></span>
  <% end %>
<% else %>
<% end %>
